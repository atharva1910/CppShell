#include <fstream>
#include <iostream>
#include <sstream>
#include <string>
#include <unistd.h>

std::string
check_alias(std::string );

bool
check_string(std::string );

std::string
execute_pwd(std::string );
